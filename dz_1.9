//1.9

#include <iostream>
#include <stdio.h>
#include <math.h>

using namespace std;

int main()
{
    float x;
    printf("input x = ");
    scanf("%f", &x);
    x = x*x;
    x = x*x;
    x = x*x;
    x = x*x;
    x = x*x;
    x = x*x;
    printf("res = %f", x);

}
