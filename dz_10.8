#include <stdio.h>
int main()
{
    char str[100];
    scanf("%s", &str);
    for (int i = 0; str[i]; i++)
    {
        while(str[i+1] == str[i])
        {
            int j = i+1;
            while(str[j] = str[j+1]) ++j;
        }
    }
    printf("%s\n",str);
}
